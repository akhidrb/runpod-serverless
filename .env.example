# Used by gradio_app/app.py to reach your deployed RunPod endpoint.
RUNPOD_API_KEY=
RUNPOD_ENDPOINT_ID=

# Used by scripts/download_weights.py to pull the gated FLUX.1-dev weights.
# Not needed by the serverless handler itself.
HF_TOKEN=
